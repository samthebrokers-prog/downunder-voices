export default async function AdminLogin({ searchParams }: { searchParams: Promise<{ error?: string }> }) { const { error } = await searchParams; return <div className="mx-auto max-w-md px-4 py-20"><form action="/api/admin/login" method="post" className="rounded-lg border bg-card p-6"><h1 className="font-serif text-2xl font-bold">Editorial login</h1><p className="mt-2 text-sm text-muted-foreground">Use the admin password stored in Vercel.</p><label className="mt-5 block text-sm font-medium">Password<input name="password" type="password" required className="mt-1 w-full rounded border border-input px-3 py-2" /></label>{error && <p className="mt-3 text-sm text-destructive">Incorrect password.</p>}<button className="mt-5 rounded bg-primary px-4 py-2 text-sm font-semibold text-primary-foreground">Sign in</button></form></div> }
